// Tailwind v4 via PostCSS. The theme itself lives in app/globals.css (@theme),
// CSS-first config — no tailwind.config.js.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
