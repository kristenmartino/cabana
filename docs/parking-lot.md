# Parking lot

Good ideas that are not in v1. Nothing enters v1 without removing something
of equal size (scope rule, docs/04-build-plan.md §3).

## v1.5 (committed fast-follows — PRD P1)
- Tech-facing daily sheet (read-only link or per-tech Telegram digest) — D7
- Member reschedule self-service within owner-defined windows
- `/needsinfo <question>` templated member follow-up from Telegram
- Photo attachments flowing through to Airtable records

## P2 (design-for, don't build — PRD P2)
- QuickBooks read integration: deposit ↔ invoice matching (`external_invoice_ref` already exists)
- Multi-tenant machinery (`business_id` already on core tables)
- Recurring one-off plans (monthly filter clean) — `kind` enum extension

## Unsorted
- (add here; date + one line of context each)
